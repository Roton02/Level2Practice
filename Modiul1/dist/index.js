"use strict";
{
    const name = 'Sabbir bro ';
    const id = 12;
    const isAdmin = false;
    const hasAccess = null;
    const hasRole = undefined;
    console.log(`name is ${name} and id is ${id} and isAdmin is ${isAdmin} and hasAccess is ${hasAccess} and hasRole is ${hasRole}`);
}
